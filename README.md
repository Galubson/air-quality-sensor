# air-quality-sensor
DIY ESP32 air quality sensor (SPS30 + BME280 + SHT40) with a local web dashboard, PM2.5-based control of a Shelly relay for ventilation, optional Home Assistant integration via MQTT, and one-click browser firmware install. No cloud, no app required.
